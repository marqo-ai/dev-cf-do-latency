{
  "name": "cf-do-latency-repro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler types --check && tsc --noEmit && vitest run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "probe": "node scripts/probe.mjs",
    "test": "vitest run",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "@types/node": "26.2.0",
    "typescript": "~5.9.0",
    "vitest": "latest",
    "wrangler": "4.125.0"
  }
}
